  <h2> Add a new book </h2>

  <%= form_for Book.new do |f| %>
  <% if @book.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@book.errors.count, "error") %> prohibited this book from being submitted:</h2>
    <ul>
      <% @book.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= f.label :title %><br />
    <%= f.text_field :title %>
  </div>

  <div>
    <%= f.label :author %><br />
    <%= f.text_field :author %>
  </div>

  <div>
    <%= f.label :year %><br />
    <%= f.text_field :year %>
  </div>

  <div>
    <%= f.label :category %><br />
    <%= f.text_field :category %>
  </div>

  <div>
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>

  <div><%= f.submit "Add New Book", class: "button active" %></div>

  <div><%= link_to 'Back', books_path %></div>
<% end %>

